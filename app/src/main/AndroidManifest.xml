<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.mamadou.diallo.healthapp">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".controller.MyAppointmentActivity"></activity>
        <activity android:name=".controller.MakeAnAppointmentActivity" />
        <activity android:name=".controller.ItineraireActivity" />
        <activity android:name=".controller.ValidationRDVActivity" />
        <activity android:name=".controller.ChoisirCreneauActivity" />
        <activity android:name=".controller.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".controller.LoginActivity" />
        <activity android:name=".controller.SignupActivity" />
        <activity android:name=".controller.SettingLanguageActivity" />
        <activity android:name=".controller.HomePageActivity" />
        <activity android:name=".controller.DisponibilityActivity" />
    </application>

</manifest>